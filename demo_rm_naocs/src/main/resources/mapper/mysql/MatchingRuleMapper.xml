<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo1_nacos.mapper.mysql.MatchingRuleMapper">

    <select id="findByWrapper" resultMap="mybatis-plus_MatchingRule">
        SELECT *
        FROM matching_rule
        ${ew.customSqlSegment}
    </select>

</mapper>